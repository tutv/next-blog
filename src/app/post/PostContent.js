function PostContent() {
    return (
        <div className="PostContent">
            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>

            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>

            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>
            <h3>Static Generation with the hello world</h3>


            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>


            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>

            <h3>Static Generation with the hello world</h3>

            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>


            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>


            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>


            <p spellCheck="true" placeholder="Empty quote" data-content-editable-leaf="true" contentEditable="false">It
                written in Notion and pre-rendered on this page using Next.js getInitialProps. It also uses Incremental
                Static Generation with the <span data-token-index="1" spellCheck="false"
                                                 className="notion-enable-hover">validate</span> option so
                the content updates when the source is edited.
            </p>

        </div>
    )
}

export default PostContent

